import { Client } from "pg";

const db = new Client({
  user: "postgres",
  host: "localhost",
  database: "auth",
  password: "$chOOl22",
  port: 5432,
});

await db.connect();

// Create the table expected by /sets routes if missing.
await db.query(`
  CREATE TABLE IF NOT EXISTS public.sets (
    id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    topic text NOT NULL DEFAULT 'Custom Set',
    title text NOT NULL,
    cards integer NOT NULL DEFAULT 0,
    color text NOT NULL DEFAULT '#FF9B00'
  );
`);

await db.end();

console.log("OK: ensured public.sets exists");

